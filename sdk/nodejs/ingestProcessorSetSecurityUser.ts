// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Sets user-related details (such as `username`, `roles`, `email`, `fullName`, `metadata`, `apiKey`, `realm` and `authenticationTyp`e) from the current authenticated user to the current document by pre-processing the ingest. The `apiKey` property exists only if the user authenticates with an API key. It is an object containing the id, name and metadata (if it exists and is non-empty) fields of the API key. The realm property is also an object with two fields, name and type. When using API key authentication, the realm property refers to the realm from which the API key is created. The `authenticationType property` is a string that can take value from `REALM`, `API_KEY`, `TOKEN` and `ANONYMOUS`.
 *
 * See: https://www.elastic.co/guide/en/elasticsearch/reference/current/ingest-node-set-security-user-processor.html
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as elasticstack from "@pulumi/elasticstack";
 * import * as elasticstack from "@tonkean/pulumi-elasticstack";
 *
 * const user = elasticstack.IngestProcessorSetSecurityUser({
 *     field: "user",
 *     properties: [
 *         "username",
 *         "realm",
 *     ],
 * });
 * const myIngestPipeline = new elasticstack.IngestPipeline("myIngestPipeline", {processors: [user.then(user => user.json)]});
 * ```
 */
export function ingestProcessorSetSecurityUser(args: IngestProcessorSetSecurityUserArgs, opts?: pulumi.InvokeOptions): Promise<IngestProcessorSetSecurityUserResult> {

    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("elasticstack:index/ingestProcessorSetSecurityUser:IngestProcessorSetSecurityUser", {
        "description": args.description,
        "field": args.field,
        "if": args.if,
        "ignoreFailure": args.ignoreFailure,
        "onFailures": args.onFailures,
        "properties": args.properties,
        "tag": args.tag,
    }, opts);
}

/**
 * A collection of arguments for invoking IngestProcessorSetSecurityUser.
 */
export interface IngestProcessorSetSecurityUserArgs {
    /**
     * Description of the processor.
     */
    description?: string;
    /**
     * The field to store the user information into.
     */
    field: string;
    /**
     * Conditionally execute the processor
     */
    if?: string;
    /**
     * Ignore failures for the processor.
     */
    ignoreFailure?: boolean;
    /**
     * Handle failures for the processor.
     */
    onFailures?: string[];
    /**
     * Controls what user related properties are added to the `field`.
     */
    properties?: string[];
    /**
     * Identifier for the processor.
     */
    tag?: string;
}

/**
 * A collection of values returned by IngestProcessorSetSecurityUser.
 */
export interface IngestProcessorSetSecurityUserResult {
    /**
     * Description of the processor.
     */
    readonly description?: string;
    /**
     * The field to store the user information into.
     */
    readonly field: string;
    /**
     * Internal identifier of the resource.
     */
    readonly id: string;
    /**
     * Conditionally execute the processor
     */
    readonly if?: string;
    /**
     * Ignore failures for the processor.
     */
    readonly ignoreFailure?: boolean;
    /**
     * JSON representation of this data source.
     */
    readonly json: string;
    /**
     * Handle failures for the processor.
     */
    readonly onFailures?: string[];
    /**
     * Controls what user related properties are added to the `field`.
     */
    readonly properties?: string[];
    /**
     * Identifier for the processor.
     */
    readonly tag?: string;
}
/**
 * Sets user-related details (such as `username`, `roles`, `email`, `fullName`, `metadata`, `apiKey`, `realm` and `authenticationTyp`e) from the current authenticated user to the current document by pre-processing the ingest. The `apiKey` property exists only if the user authenticates with an API key. It is an object containing the id, name and metadata (if it exists and is non-empty) fields of the API key. The realm property is also an object with two fields, name and type. When using API key authentication, the realm property refers to the realm from which the API key is created. The `authenticationType property` is a string that can take value from `REALM`, `API_KEY`, `TOKEN` and `ANONYMOUS`.
 *
 * See: https://www.elastic.co/guide/en/elasticsearch/reference/current/ingest-node-set-security-user-processor.html
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as elasticstack from "@pulumi/elasticstack";
 * import * as elasticstack from "@tonkean/pulumi-elasticstack";
 *
 * const user = elasticstack.IngestProcessorSetSecurityUser({
 *     field: "user",
 *     properties: [
 *         "username",
 *         "realm",
 *     ],
 * });
 * const myIngestPipeline = new elasticstack.IngestPipeline("myIngestPipeline", {processors: [user.then(user => user.json)]});
 * ```
 */
export function ingestProcessorSetSecurityUserOutput(args: IngestProcessorSetSecurityUserOutputArgs, opts?: pulumi.InvokeOptions): pulumi.Output<IngestProcessorSetSecurityUserResult> {
    return pulumi.output(args).apply((a: any) => ingestProcessorSetSecurityUser(a, opts))
}

/**
 * A collection of arguments for invoking IngestProcessorSetSecurityUser.
 */
export interface IngestProcessorSetSecurityUserOutputArgs {
    /**
     * Description of the processor.
     */
    description?: pulumi.Input<string>;
    /**
     * The field to store the user information into.
     */
    field: pulumi.Input<string>;
    /**
     * Conditionally execute the processor
     */
    if?: pulumi.Input<string>;
    /**
     * Ignore failures for the processor.
     */
    ignoreFailure?: pulumi.Input<boolean>;
    /**
     * Handle failures for the processor.
     */
    onFailures?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Controls what user related properties are added to the `field`.
     */
    properties?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Identifier for the processor.
     */
    tag?: pulumi.Input<string>;
}
